import { Redirect } from "expo-router";

export default function index() {
    return (
       <Redirect href={"/(routes)/onboarding"} />
);
   
}
//<Redirect href={"/(routes)/onboarding"} />
//<Redirect href={"/(tabs)"} />